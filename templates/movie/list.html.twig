{% extends 'base.html.twig' %}

{% block title %} Movie list - {{ parent() }}{% endblock %}

{% block body %}
    <h2 class="text-center text-red-700 text-3xl font-semibold">The last movies</h2>

    <div class="grid grid-cols-4 gap-3 mt-5 max-w-[800px] mx-auto">
        {% for movie in movies %}
            <div class="border shadow-lg">
                <img class="object-cover" src="{{ movie.cover }}" alt="">
                <div class="p-3 mt-3">
                    <h2 class="font-semibold text-lg mb-3">
                        {{ movie.title }}
                    </h2>
                    <p class="mb-3">
                        {{ movie.releasedAt | date('j/m/Y') }} |
                        {{ movie.duration }}min
                    </p>
                    <div>
                        <a class="py-1 px-2 bg-sky-700 rounded text-white font-semibold inline-block" href="{{ path('movie_show', {'id': movie.id}) }}">Voir</a>
                    </div>
                </div>
                
            </div>
        {% endfor %}
    </div>
{% endblock %}
